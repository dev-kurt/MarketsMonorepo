info:
  name: Trending
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/search/trending"
  headers:
    - name: accept
      value: application/json
    - name: x-cg-demo-api-key
      value: "{{cg_demo_api_key}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Trending başarılı", function () {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true

docs: |-
  O an trend olan coin'ler (arama hacmine göre).

  - **Metot:** `GET`
  - **URL:** `{{base_url}}/search/trending`

  ## Yanıttaki kilit alanlar
  `coins[].item.{id, name, symbol, thumb, market_cap_rank}`
